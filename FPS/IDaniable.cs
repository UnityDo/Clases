using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IDaniable
{
    //Definimos el metodo general de la propiedad o interface
    //El interface no hace nada, mas q dar una definicion
    //se implemente en la clase que tenga la propiedad
    void RecibeDanio(float danio);
}
   
